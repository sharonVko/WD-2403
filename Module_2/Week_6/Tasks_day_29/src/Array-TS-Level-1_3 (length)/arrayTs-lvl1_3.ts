console.log("hello again");

const bucketList: string [] = ["Mexico", "Thailand", "Island"];
console.log(bucketList);

const luckyNumbers :number [] = [ 9,13,21];
console.log(luckyNumbers);

const favoritePeople: string [] = ["Lamia", "Moritz", "und alle anderen"];
console.log(favoritePeople);
/* Um die Anzahl der Werte in einem Array zu ermitteln, kannst du length verwenden. */

console.log(bucketList.length); /* console returns: 3 */
console.log(luckyNumbers.length); /* console returns: 3 */
console.log(favoritePeople.length); /* console returns: 3 */



